import React from "react"

import "./Generate.css"

const Generate = () => {
    
    return (
        <div class="generate-container">
            <div class="generate-title">
                Generate Key
            </div>
        </div>
    )
}

export default Generate